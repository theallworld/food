# 运行铁律与核心准则 (Operating Iron Rules)

## 核心铁律一：遇到不懂/不确定的问题，坚决先上网搜索，严禁靠猜

1. **绝对禁令**：
   - 在面对未经验证的技术细节、新模型特性（如 API 隐藏参数、模型默认行为）、偶发性报错原因、小众开源库用法、硬件行情及任何未完全确认的事实时，**绝对禁止凭借过时记忆或主观猜测直接下结论**。

2. **强制动作（全网搜索先行）**：
   - 只要遇到任何不确定或疑难问题，**必须第一时间主动调用联网工具（`search_web`、`read_url_content` 等）进行全网多源检索与核实**。
   - **重点检索渠道**包括但不限于：
     - **GitHub**（Issues、Pull Requests、Discussions、开源源码）
     - **Google**（最新官方技术文档、权威博客、错误日志分析）
     - **X (Twitter)**（开发者最新实测、官方发布的第一手消息）
     - **Reddit**（真实用户实测讨论、踩坑踩雷记录）
     - **YouTube**（最新教程、测试对比视频）
     - **Wikipedia / 维基百科**（基础定义与权威事实核验）

3. **求证闭环原则**：
   - 查证到确凿答案或官方规范后，方可推进代码实现或向用户解答。
   - 凡遇异常，先复现、先查证、再解决，确保方案 100% 真实可靠。

---

## 核心铁律二：版本发布与交付必须显式携带版本号 (Versioning)

1. **版本递增规范**：
   - 每次修复重大问题、重构交互或新增功能发布安装包时，**必须递增并明确标注语义化版本号（如 v1.0, v1.1, v1.2）**。
2. **文件名强区分**：
   - **交付的安装包文件名必须包含版本号**（例如 `食物热量助手_v1.1.apk`），严禁只保留无版本号的同名文件覆盖导致用户难以分辨新旧文件。
3. **界面可视回显**：
   - App 内部界面（如首页顶部、设置页或关于页）必须同步呈现当前生效的版本号，让用户一目了然确认是否已更新到最新版。
